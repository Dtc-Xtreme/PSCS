@using PSCS.Domain;

@model ZoneSearchViewModel

<div class="card">
    <div class="card-header">
        <div class="row">
            <div class="col-auto">
                <a class="btn btn-primary" asp-controller="Zone" asp-action="Add">+</a>
            </div>
            <div class="col">
                <h4 class="pt-1 m-0">Zones</h4>
            </div>
            <div class="col-auto">
                <form method="get" action="/Zone?Search=@Model.Search">
                    <div class="input-group">
                        <input type="text" class="form-control" asp-for="@Model.Search" placeholder="Name" />
                        <button type="submit" class="btn btn-primary">Search</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <div class="card-body overflow-auto">
        <table class="table table-striped table-hover">
            <thead>
                <tr>             
                    <th class="text-center">ID</th>
                    <th class="">Name</th>
                    <th class="text-center">Edit</th>
                    <th class="text-center">Delete</th>
                </tr>
            </thead>
            <tbody>
                @if(Model.Zones != null)
                {
                    foreach (Zone zone in Model.Zones)
                    {
                        <tr>
                            <td class="text-center align-middle">@zone.Id</td>
                            <td class="align-middle">@zone.Name</td>
                            <td class="text-center align-middle"><a class="btn btn-warning" asp-controller="Zone" asp-action="Edit" asp-route-id="@zone.Id">Edit</a></td>
                            <td class="text-center align-middle"><a class="btn btn-danger" asp-controller="Zone" asp-action="Delete" asp-route-id="@zone.Id">X</a></td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>